{
  "name": "mtproxy-launcher",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bash scripts/build.sh",
    "test": "node --test tests/*.test.mjs && bash tests/shell/unit.sh",
    "test:cores": "bash tests/shell/cores.sh",
    "test:release": "node tests/check-release-assets.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.110.0"
  }
}
